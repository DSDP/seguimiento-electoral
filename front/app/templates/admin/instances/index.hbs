<article>
  
  
</article>
<h1>Listado de instances</h1>

<p>
  {{link-to "Crear nuevo rol" "admin.instances.new" class="btn"}}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each instance in model}}
        <tr>
          <td>
            {{instance.name}}
          </td>
          <td>
            {{link-to "Editar" "admin.instances.edit" instance}}
          </td>
          <td>
            <a href="#" {{action "remove" instance}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
