<article>
  
  
</article>
<h1>Listado de fuerzas politicas</h1>

<p>
  {{link-to "Crear nueva fuerza politica" "data-entry.forces.new" class="btn"}}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each force in model}}
        <tr>
          <td>
            {{force.name}}
          </td>
          <td>
            {{link-to "Editar" "data-entry.forces.edit" force}}
          </td>
          <td>
            <a href="#" {{action "remove" force}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
