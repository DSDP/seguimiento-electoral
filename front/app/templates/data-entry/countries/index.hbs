<article>
  
  
</article>
<h1>Listado de pais</h1>

<p>
  {{link-to "Crear nuevo pais" "data-entry.countries.new" }}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each country in model}}
        <tr>
          <td>
            {{country.name}}
          </td>
          <td>
            {{link-to "Editar" "data-entry.countries.edit" country}}
          </td>
          <td>
            <a href="#" {{action "remove" country}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
