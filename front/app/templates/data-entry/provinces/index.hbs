<article>
  
  
</article>
<h1>Listado de provincias</h1>

<p>
  {{link-to "Crear nuevo provincia" "data-entry.provinces.new" }}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each province in model}}
        <tr>
          <td>
            {{province.name}}
          </td>
          <td>
            {{link-to "Editar" "data-entry.provinces.edit" province}}
          </td>
          <td>
            <a href="#" {{action "remove" province}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
