<article>
  
  
</article>
<h1>Listado de Referentes</h1>

<p>
  {{link-to "Crear nuevo Referente" "data-entry.referrings.new" class="btn"}}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each referring in model}}
        <tr>
          <td>
            {{referring.fullName}}
          </td>
          <td>
            {{link-to "Editar" "data-entry.referrings.edit" referring}}
          </td>
          <td>
            <a href="#" {{action "remove" referring}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
