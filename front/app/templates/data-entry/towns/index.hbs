<article>
  
  
</article>
<h1>Listado de distritos</h1>

<p>
  {{link-to "Crear nuevo distrito" "data-entry.towns.new" }}
</p>

{{#if model.length}}
  <table class="table-minimal">
    <thead>
      <tr>
        <th>
          Nombre
        </th>
        <th colspan="3">
          Acciones
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each town in model}}
        <tr>
          <td>
            {{town.name}}
          </td>
          <td>
            {{link-to "Editar" "data-entry.towns.edit" town}}
          </td>
          <td>
            <a href="#" {{action "remove" town}}>Eliminar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <div class="loading">
  {{loading-bubbles color='#0083ff' size=100}}
</div>
{{/if}}
