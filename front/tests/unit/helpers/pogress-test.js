import { pogress } from '../../../helpers/pogress';
import { module, test } from 'qunit';

module('Unit | Helper | pogress');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = pogress(42);
  assert.ok(result);
});
